<!-- Index.cshtml -->
@model IEnumerable<LibraryManagement.Models.Books>

<h1>Books List</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Branch</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.BookId</td>
                <td>@book.Title</td>
                <td>@(book.Branch != null ? book.Branch.BranchName : "N/A")</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@book.BookId">Edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>
